# M06 对话编排 — 子任务清单

> 负责 Agent 对话流程控制，按 SKILL.md 规范响应用户自然语言请求。
> 关联文件：`SKILL.md`、`scripts/qianniu_analytics_orchestrator.py`

---

## 已完成

- [x] D-01: 设计对话状态机（预检 → 取数 → 报告 → 推送）
- [x] D-02: 实现场景 A 首次初始化指引（一次性输出 3 项前置条件）
- [x] D-03: 实现场景 B 并行预检（授权/订购/绑店一次性检查）
- [x] D-04: 实现非淘系诉求明确拒绝逻辑
- [x] D-05: 实现对话中报告全文回显（禁止仅回复"已生成"）

---

## 待完成

> 本模块当前无高优先级任务，核心流程已完整。

### P2 — 可选增强
- [ ] D-06: 增加对话上下文记忆（同一轮对话中记住已确认的店铺/时间范围）
  - 测试点：用户说"换成上周"时自动复用已确认的店铺
  - 验收：对话上下文中保留最近确认的 `shopIds` 和 `dataType`

- [ ] D-07: 增加自然语言日期解析增强（支持"大前天""上上周"等口语）
  - 测试点："大前天"解析为前天，"上上周"解析为上周再往前一周
  - 验收：日期解析函数覆盖常见口语表达

- [ ] D-08: 增加追问澄清策略（店名歧义时展示候选列表供用户选择）
  - 测试点：用户说"旗舰店"但绑定多家旗舰店时，列出候选让用户确认
  - 验收：候选列表含 `shopName` + 部分 `id` 掩码

---

*模块状态：✅ 已完成 | 完成：5/5（3 个可选增强待评估）*
