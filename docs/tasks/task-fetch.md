# M02 淘系取数引擎 — ~~子任务清单~~（已废弃）

> ~~负责执行 JYCM OpenAPI A→G 接口链，获取生意参谋数据并下载 Excel。~~
> **2026-06-25 废弃**：API 取数路线已确认放弃，当前为 Excel 驱动模式。取数脚本 `jycm_fetch_sycm_shop.py` 已删除。
> 保留此文件供历史参考，所有子任务标记为不适用。

---

## 已完成

- [x] F-01: 实现 A→G 接口链完整调用（getChannelList → createAndDownload）
- [x] F-02: 实现 `channelName=天猫淘宝` 固定锚点
- [x] F-03: 实现 `shopIds` 字符串数组构造与传递
- [x] F-04: 实现日期范围 ISO8601 格式化（`T00:00:00+08:00`）
- [x] F-05: 实现 Excel 下载 URL 提取与保存

---

## 待完成

### P1 — 高优先级
- [ ] F-06: **支持更多 dataType / dataDimension 组合**
  - 当前硬编码 `dataType=店铺`、`dataDimension=整体`
  - 目标：支持 `商品/品类/内容/客户/推荐分析` 等类型
  - 测试点：不同 dataType 的接口链能正确执行
  - 验收：`jycm_fetch_sycm_shop.py` 支持通过参数指定 dataType 和 dataDimension

- [ ] F-07: **接口失败重试机制（指数退避）**
  - 测试点：网络超时/5xx 时自动重试 3 次
  - 验收：重试间隔 1s → 2s → 4s，最终失败时报明确错误

### P2 — 功能增强
- [ ] F-08: 实现异步并行取数（多店铺同时请求）
  - 测试点：3 家店铺取数总耗时 < 串行耗时
  - 验收：`asyncio` 或 `concurrent.futures` 实现

- [ ] F-09: 增加取数结果缓存（相同参数 1 小时内不重复取数）
  - 测试点：相同店铺+日期范围第二次取数直接返回缓存
  - 验收：`data/` 目录下缓存文件按参数哈希命名

- [ ] F-10: 增加取数进度反馈（长耗时任务分阶段输出进度）
  - 测试点：用户能看到 "步骤 A 完成 → 步骤 B 完成 ..."
  - 验收：每完成一个步骤输出进度百分比

---

## 依赖关系

```
F-01 → F-02, F-03, F-04, F-05
F-06 依赖 F-01（基础接口链）
F-07 依赖 F-01（在基础链上加重试）
F-08 依赖 F-01
F-09 依赖 F-05（下载后才缓存）
```

---

*模块状态：🔄 维护中 | 完成：5/10*
